Place emulator or desktop-test shared libraries here, for example:
- libnode.so
- vendor-specific JNI dependencies required by Python/Node/native services

Keep ABI contents aligned with the filters declared in app/build.gradle.
