Commit the ARM64 Node.js runtime here:
- libnode.so

This APK template expects libnode.so to be bundled at build time so app users never have to place it manually after install.
Keep this ABI aligned with the filters declared in app/build.gradle.
